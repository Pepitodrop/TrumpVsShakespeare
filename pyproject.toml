[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "trump-vs-shakespeare"
version = "1.0.2"
description = "A production-ready 1v1 web game powered by TrumpScript, Shakespeare Programming Language, and Assembly."
readme = "README.md"
requires-python = ">=3.11"
license = { file = "LICENSE" }
authors = [{ name = "Pepitodrop" }]
dependencies = [
  "fastapi>=0.115,<1.0",
  "uvicorn[standard]>=0.30,<1.0"
]

[project.optional-dependencies]
dev = [
  "httpx>=0.27,<1.0",
  "pip-audit>=2.7,<3.0",
  "pytest>=8.3,<9.0",
  "ruff>=0.9,<1.0"
]

[project.scripts]
trump-vs-shakespeare = "trump_vs_shakespeare.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
trump_vs_shakespeare = [
  "assets/programs/*.tr",
  "assets/programs/*.spl",
  "web/static/*"
]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
